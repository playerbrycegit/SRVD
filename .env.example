# Stage 4 §22 environment variable convention. Copy to .env for local dev.
PORT=4001
WEB_PORT=4002
# In a networked environment with Postgres, this replaces node:sqlite (see src/shared-kernel/db.js):
# DATABASE_URL=postgres://user:pass@localhost:5432/station
SESSION_TTL_DAYS=30
