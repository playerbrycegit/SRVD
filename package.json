{
  "name": "station",
  "version": "0.1.0-foundation",
  "private": true,
  "description": "STATION - professional operating system for bartenders. V1 scope: Auth, Home, Tools, Vault.",
  "engines": { "node": ">=22.5.0" },
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev:api": "node scripts/dev-server.js",
    "dev:web": "node -e \"require('./src/http/static-server').createStaticServer().listen(4002, () => console.log('Web on http://localhost:4002'))\"",
    "migrate": "node -e \"const {createDb,runMigrations}=require('./src/shared-kernel/db');const db=createDb();console.log('Applied:', runMigrations(db));\"",
    "lint": "echo 'ESLint is not installed in this sandbox (no network access to npm registry). See README > Remaining Work.' && exit 0",
    "format": "echo 'Prettier is not installed in this sandbox (no network access to npm registry). See README > Remaining Work.' && exit 0",
    "typecheck": "echo 'TypeScript is not installed in this sandbox (no network access to npm registry). See README > Remaining Work.' && exit 0"
  },
  "dependencies": {},
  "devDependencies": {},
  "_intendedDependencies_seeStage4Section2": {
    "note": "Stage 4 §2 specifies these; not installed here because npm registry access returns 403 in this sandbox (verified during Stage 11 foundation setup). The current implementation uses zero dependencies (Node built-ins only) so it runs today; installing these in a networked environment is additive, not a rewrite.",
    "backend-framework": "fastify (or equivalent minimal Node framework)",
    "orm": "prisma (or equivalent schema-first ORM) - would replace src/shared-kernel/db.js's direct node:sqlite calls with Postgres",
    "database-driver": "pg",
    "validation": "zod (current hand-written validation in src/shared-kernel/validation is functionally equivalent and dependency-free)",
    "testing": "none needed - node:test (built-in) is sufficient and already in use",
    "linting": "eslint",
    "formatting": "prettier",
    "typechecking": "typescript (with checkJs, or a full .ts migration)"
  }
}
